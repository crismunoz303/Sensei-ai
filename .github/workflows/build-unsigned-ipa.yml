name: Build SENSEI unsigned IPA

on:
  workflow_dispatch:
  push:
    branches: [ main, sensei-local-persistence-drive ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: macos-26

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Ensure Metal toolchain
        run: |
          if ! xcrun metal --version >/dev/null 2>&1; then
            sudo xcodebuild -downloadComponent MetalToolchain
          fi
          xcrun metal --version

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Build for iPhone without code signing
        run: |
          xcodebuild \
            -project Sensei.xcodeproj \
            -scheme Sensei \
            -configuration Release \
            -sdk iphoneos \
            -derivedDataPath build \
            -skipPackagePluginValidation \
            -skipMacroValidation \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            build

      - name: Package unsigned IPA
        run: |
          rm -rf Payload
          mkdir -p Payload
          cp -R build/Build/Products/Release-iphoneos/SENSEI.app Payload/
          zip -r SENSEI-V2-MLX-unsigned.ipa Payload

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: SENSEI-V2-MLX-unsigned
          path: SENSEI-V2-MLX-unsigned.ipa
